services:
  altcha-backend:
    build: ./backend
    ports:
      - "4000:4000"
    depends_on:
      - redis
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    restart: unless-stopped
