version: "3.8"
services:
  altcha-backend:
    build: ./backend
    ports:
      - "4000:4000"
    environment:
      - ALTCHA_SECRET=${ALTCHA_SECRET}
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS}
    restart: unless-stopped
