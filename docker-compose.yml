services:
  altcha-backend:
    build: ./backend
    ports:
      - "4000:4000"
    restart: unless-stopped
    volumes:
      - ./backend:/usr/src/app
      - /usr/src/app/node_modules

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    restart: unless-stopped
